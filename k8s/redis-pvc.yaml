apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bunny-redis-pvc
  namespace: bunny-game
  labels:
    app: bunny-redis
    component: cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ""  # Use default storage class