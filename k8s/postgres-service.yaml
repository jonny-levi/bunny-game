apiVersion: v1
kind: Service
metadata:
  name: bunny-postgres
  namespace: bunny-game
  labels:
    app: bunny-postgres
    component: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgresql
  selector:
    app: bunny-postgres